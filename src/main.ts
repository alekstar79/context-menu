import { createApp } from 'vue'
import App from './App.vue'
import './scss/styles.scss'

/**
* It supports two-way initialization,
* either as a vanilla ts/js script or as a vue component.
*/

createApp(App).mount('#app')
